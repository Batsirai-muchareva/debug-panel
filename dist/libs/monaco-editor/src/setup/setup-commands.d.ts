import type { Monaco } from '@monaco-editor/react';
import type { editor } from 'monaco-editor';
export declare function setupCommands(editor: editor.IStandaloneCodeEditor, monaco: Monaco, activateSearch: (status: boolean) => void): void;
